// **no main, crie dois vetores de capacidade 100 (utilize uma constante para a capacidade) chamados vetOrig e vetOrd (para o vetor original e vetor ordenado, respectivamente).


// ***no main, exiba um menu com as seguintes opções, onde cada opção deve ser implementada numa função a parte, isto é, fora do main:

// -insira um elemento digitado pelo usuário no final do vetor (verificando se é possível ou não inserir este elemento, e alterando o tamanho atual do vetor (a função inserir retorna o novo tamanho do vetor)
// -crie o vetor ordenado baseado no vetor original, para isso, você irá implementar uma função inserirOrdenado (igual a vista em sala de aula), depois irá percorrer o vetor original, e para cada elemento do vetor original, irá chamar a função inserirOrdenado para inserir o elemento atual do vetor original no vetor ordenado
// -realize uma busca binária no vetor ordenado solicitando que o usuário digite o valor a ser buscado, e imprima se achou ou não o valor, e se achou, em que posição esse elemento se encontra.

